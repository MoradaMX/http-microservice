version: '3.8'
services:
  microservice:
    restart: always
    container_name: microservice
    build:
      context: ../..
      dockerfile: docker/production/Dockerfile
    env_file:
      - ../../.env
    ports:
      - ${PORT}:${PORT}
    networks:
      - mokalli

networks:
  mokalli:
    external: true
